apiVersion: config.projectsveltos.io/v1beta1
kind: ClusterSummary
metadata:
  labels:
    app.kubernetes.io/name: addon-controller
    app.kubernetes.io/managed-by: kustomize
  name: clustersummary-sample
spec:
  # TODO(user): Add fields here
