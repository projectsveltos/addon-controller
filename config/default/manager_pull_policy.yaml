apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller
  namespace: projectsveltos
spec:
  template:
    spec:
      initContainers:
      - name: initialization
        imagePullPolicy: 
      containers:
      - name: controller
        imagePullPolicy: 
